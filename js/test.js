const arrW = 6;
const arrH = 4;
const corners = [0, 200, 50, 100];

function step(from, to, size) {
	return (to - from) / (size - 1);
}

(function() {
	const colors = [];
	const left = [];
	const right = [];

	for (let i = 0; i < arrH; i++) {
		colors[i] = [];
		for (let j = 0; j < arr; j++) {

		}
	}
})();